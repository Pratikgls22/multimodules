#spring.application.name=females-in-tech

server:
  port: 9090


# Datasource properties
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/females_in_tech_db?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

# Jpa properties
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  # Mail configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: "pratikpanchal5980@gmail.com"
    password: "pfjn ifyz voza aauz"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
#        debug: true

# Login URL
login.url : "http://localhost:8080/"